import 'package:flutter/material.dart';

class AppColors {
  static const bubbleBlue = Color(0xFF4DD0E1);

  // Light
  static const lightBackground = Color.fromARGB(255, 203, 243, 248);
  static const lightThemePrimary = Color(0xFF00ACC1);
  static const lightThemeSecondary = Color(0xFF4DD0E1);

  // Dark
  static const darkBackground = Color(0xFF212121);
  static const darkThemePrimary = Color(0xFF00BCD4);
  static const darkThemeSecondary = Color(0xFF00BCD4);
}
